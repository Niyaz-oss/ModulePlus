from setuptools import setup, find_packages

setup(
    name="moduleplus",
    version="1.0.0",
    description="A Python mega-module with useful modules and helper functions",
    packages=find_packages(),
    python_requires=">=3.8",
)
